Extras and next steps

- Add a richer ML pipeline (TF-IDF + classifier or transformer-based models).
- Replace the heuristic solver with a trained model and add dataset handling.
- Add telemetry opt-in and logging (suggest python-dotenv for config).
