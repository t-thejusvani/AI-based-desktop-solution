Developer notes

- Use `.venv` and install from `requirements.txt`.
- To train the toy rules model: `python scripts/train_model.py`.
- To run tests: `pytest -q`.
- To build Windows executable (Windows PowerShell): `scripts\\build_windows.ps1`.
